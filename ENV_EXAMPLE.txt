# Скопируйте это содержимое в файл .env в папке printcore-backend

# База данных
# Для Railway PostgreSQL (рекомендуется для production):
# DATABASE_URL создается автоматически Railway при добавлении PostgreSQL сервиса
# Не нужно устанавливать вручную - Railway делает это автоматически

# Для локальной разработки (SQLite):
DB_FILE=printcore.db

# JWT авторизация
JWT_SECRET=printcore_secret_key_2025_change_me_in_production
JWT_EXPIRES_IN=7d

# Сервер
PORT=3001
NODE_ENV=development

# Frontend URL для CORS
FRONTEND_URL=http://localhost:3000

