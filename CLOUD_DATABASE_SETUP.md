# ☁️ Настройка облачной базы данных

## Вариант 1: Supabase (Рекомендуется) ⭐

### Шаг 1: Создайте аккаунт
1. Перейдите на https://supabase.com
2. Нажмите "Start your project"
3. Войдите через GitHub

### Шаг 2: Создайте проект
1. Нажмите "New Project"
2. Выберите организацию
3. Заполните:
   - **Name**: `printcore-db`
   - **Database Password**: `придумайте сложный пароль`
   - **Region**: `Frankfurt (eu-central-1)` или ближайший к вам
   - **Pricing Plan**: `Free` (500 MB)
4. Нажмите "Create new project" (подождите ~2 минуты)

### Шаг 3: Получите Connection String
1. В боковом меню откройте "Project Settings" ⚙️
2. Выберите "Database"
3. Прокрутите до "Connection string"
4. Выберите "URI" и скопируйте строку:

```
postgresql://postgres:[YOUR-PASSWORD]@db.xxx.supabase.co:5432/postgres
```

### Шаг 4: Обновите .env

```env
# Замените на свои данные из Supabase
DB_HOST=db.xxx.supabase.co
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=ваш-пароль-из-шага-2
DB_NAME=postgres
```

**Или используйте DATABASE_URL:**
```env
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.xxx.supabase.co:5432/postgres
```

---

## Вариант 2: Render

### Шаг 1: Создайте аккаунт
1. Перейдите на https://render.com
2. Зарегистрируйтесь через GitHub

### Шаг 2: Создайте PostgreSQL
1. На Dashboard нажмите "New +" → "PostgreSQL"
2. Заполните:
   - **Name**: `printcore-db`
   - **Database**: `printcore`
   - **User**: `printcore`
   - **Region**: выберите ближайший
   - **Plan**: `Free`
3. Нажмите "Create Database"

### Шаг 3: Получите данные подключения
После создания вы увидите:
- **Internal Database URL** (используйте этот!)
- **External Database URL**
- **PSQL Command**

### Шаг 4: Обновите .env

Скопируйте данные из "Connections":
```env
DB_HOST=xxx.oregon-postgres.render.com
DB_PORT=5432
DB_USERNAME=printcore
DB_PASSWORD=автогенерированный-пароль
DB_NAME=printcore
```

---

## Вариант 3: Railway

### Шаг 1: Создайте аккаунт
1. https://railway.app
2. Sign up через GitHub

### Шаг 2: Создайте проект
1. "New Project"
2. "Provision PostgreSQL"
3. Дождитесь создания

### Шаг 3: Получите переменные
В разделе "Variables" скопируйте:
- `PGHOST`
- `PGPORT`
- `PGUSER`
- `PGPASSWORD`
- `PGDATABASE`

### Шаг 4: Обновите .env

```env
DB_HOST=из PGHOST
DB_PORT=из PGPORT
DB_USERNAME=из PGUSER
DB_PASSWORD=из PGPASSWORD
DB_NAME=из PGDATABASE
```

---

## ✅ После настройки

1. Обновите `.env` файл
2. Запустите backend:
   ```bash
   npm run start:dev
   ```

3. Проверьте подключение:
   - Если видите "Database connected" - всё работает!
   - Таблицы создадутся автоматически при первом запуске

---

## 🧪 Тестирование подключения

Запустите сервер и проверьте логи:

```bash
npm run start:dev
```

Вы должны увидеть:
```
🚀 Server is running on: http://localhost:3001
📚 Swagger docs: http://localhost:3001/api/docs
```

Если ошибка подключения - проверьте:
- Правильность хоста
- Пароль
- Порт (обычно 5432)
- Firewall/Security группы

---

## 📊 Сравнение провайдеров

| Провайдер | Free Tier | Регион | Скорость | Удобство |
|-----------|-----------|--------|----------|----------|
| **Supabase** | 500 MB | 🇪🇺 EU + 🇺🇸 US | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |
| **Render** | 90 дней | 🇺🇸 US | ⭐⭐⭐⭐ | ⭐⭐⭐⭐ |
| **Railway** | $5/мес | 🇺🇸 US + 🇪🇺 EU | ⭐⭐⭐⭐⭐ | ⭐⭐⭐⭐⭐ |

**Рекомендация**: Supabase - лучший баланс функций и удобства!

---

**Выберите провайдер и следуйте инструкциям! 🚀**





