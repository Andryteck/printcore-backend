# ⚡ Быстрое решение: База данных очищается при деплое

## 🎯 Проблема
При каждом деплое на Railway база данных SQLite очищается.

## ✅ Решение за 5 минут

### Вариант 1: Railway PostgreSQL (Рекомендуется)

1. Откройте Railway Dashboard: https://railway.app
2. Выберите проект `printcore-backend`
3. Нажмите **"+ New"** → **"Database"** → **"Add PostgreSQL"**
4. Railway автоматически создаст `DATABASE_URL`
5. В **Variables** добавьте: `DB_SYNC=true`
6. **Redeploy**

### Вариант 2: Внешний PostgreSQL (Supabase/Neon) - Альтернатива

1. Создайте проект на https://supabase.com или https://neon.tech
2. Скопируйте **Connection string** (URI)
3. В Railway **Variables** добавьте:
   ```
   DATABASE_URL=postgresql://[ваш_connection_string]
   DB_SYNC=true
   ```
4. **Redeploy**

### Вариант 3: Railway Volume для SQLite (если PostgreSQL недоступен)

1. Railway Dashboard → **Variables**
2. Добавьте:
   ```
   DB_FILE=/app/data/printcore.db
   DB_SYNC=true
   ```
3. Убедитесь, что volume `printcore-data` существует (уже настроено)
4. **Redeploy**

---

## ✅ Что изменилось в коде

- ✅ Добавлена поддержка PostgreSQL (автоматически определяется по `DATABASE_URL`)
- ✅ Локально продолжает работать SQLite
- ✅ На Railway автоматически используется PostgreSQL
- ✅ Данные сохраняются между деплоями

---

## 🔍 Проверка

После деплоя в логах должно быть:
```
📊 Используется PostgreSQL (DATABASE_URL найден)
✅ База данных инициализирована успешно!
```

---

**Подробная инструкция**: см. `RAILWAY_POSTGRESQL_SETUP.md`

