# 🐘 Настройка PostgreSQL на Railway (Решение проблемы очистки БД)

## ❌ Проблема

При каждом деплое на Railway база данных SQLite очищается, так как файловая система эфемерная.

## ✅ Решение: Переход на PostgreSQL

PostgreSQL на Railway - это отдельный сервис с постоянным хранилищем. Данные сохраняются между деплоями автоматически.

> 💡 **Альтернативы**: Если по какой-то причине Railway PostgreSQL недоступен, см. файл `ALTERNATIVE_DATABASE_SOLUTIONS.md` для альтернативных решений (Supabase, Neon, Railway Volume и др.).

---

## 📋 Пошаговая инструкция

### Шаг 1: Создать PostgreSQL сервис на Railway

1. Откройте **Railway Dashboard**: https://railway.app
2. Выберите ваш проект `printcore-backend`
3. Нажмите **"+ New"** → **"Database"** → **"Add PostgreSQL"**
4. Railway автоматически создаст PostgreSQL сервис

### Шаг 2: Получить DATABASE_URL

1. После создания PostgreSQL, Railway автоматически создаст переменную `DATABASE_URL`
2. Перейдите в **Variables** вашего backend сервиса
3. Убедитесь, что переменная `DATABASE_URL` присутствует (Railway создает её автоматически)
4. Формат: `postgresql://user:password@host:port/database`

### Шаг 3: Обновить переменные окружения

В **Variables** вашего backend сервиса должны быть:

```env
DATABASE_URL=postgresql://postgres:password@host:port/railway
# Эта переменная создается автоматически Railway при добавлении PostgreSQL

# Остальные переменные
JWT_SECRET=ваш-секретный-ключ
JWT_EXPIRES_IN=7d
PORT=3001
NODE_ENV=production
FRONTEND_URL=https://printcore.by
DB_SYNC=true  # Для автоматического создания таблиц при первом запуске
```

### Шаг 4: Установить зависимость pg

Зависимость `pg` уже добавлена в `package.json`. После следующего деплоя она установится автоматически.

### Шаг 5: Redeploy

1. Railway Dashboard → ваш backend сервис
2. **Settings** → **Redeploy**
3. Дождитесь завершения деплоя

### Шаг 6: Проверка

После деплоя проверьте логи:

```
📊 Используется PostgreSQL (DATABASE_URL найден)
🔌 Подключение к базе данных...
✅ База данных инициализирована успешно!
```

---

## 🔄 Миграция существующих данных (если нужно)

Если у вас уже есть данные в SQLite, которые нужно перенести:

### Вариант 1: Экспорт/Импорт через SQL

1. Экспортируйте данные из SQLite:
```bash
sqlite3 printcore.db .dump > backup.sql
```

2. Адаптируйте SQL для PostgreSQL (замените типы данных)
3. Импортируйте в PostgreSQL через Railway Dashboard → PostgreSQL → Query

### Вариант 2: Через TypeORM миграции

Создайте миграции для переноса данных (если нужно).

---

## 🛠️ Локальная разработка

Для локальной разработки продолжайте использовать SQLite:

1. **Не устанавливайте** `DATABASE_URL` в локальном `.env`
2. Backend автоматически использует SQLite (`printcore.db`)
3. Для production на Railway будет использоваться PostgreSQL

---

## ✅ Преимущества PostgreSQL на Railway

- ✅ **Постоянное хранилище** - данные не теряются при деплое
- ✅ **Автоматические бэкапы** - Railway делает бэкапы автоматически
- ✅ **Масштабируемость** - легко увеличить ресурсы
- ✅ **Надежность** - production-ready решение
- ✅ **Не нужно настраивать volumes** - всё работает из коробки

---

## 🔍 Проверка что всё работает

### 1. Проверьте логи Railway

Должно быть:
```
📊 Используется PostgreSQL (DATABASE_URL найден)
✅ База данных инициализирована успешно!
```

### 2. Проверьте API

Откройте Swagger: `https://ваш-railway-url.up.railway.app/api/docs`

### 3. Создайте тестового пользователя

Попробуйте зарегистрироваться через API.

### 4. Проверьте сохранение данных

1. Создайте заказ
2. Сделайте **Redeploy**
3. Проверьте, что заказ остался в базе

---

## 🐛 Если что-то не работает

### Проблема: "Cannot find module 'pg'"

**Решение**: Убедитесь, что `pg` добавлен в `package.json` и выполнен `npm install`

### Проблема: "Connection refused"

**Решение**: 
1. Проверьте, что PostgreSQL сервис запущен на Railway
2. Проверьте, что `DATABASE_URL` правильно установлен
3. Убедитесь, что backend и PostgreSQL в одном проекте Railway

### Проблема: "Table does not exist"

**Решение**: 
1. Установите `DB_SYNC=true` в Variables
2. Сделайте Redeploy
3. TypeORM автоматически создаст таблицы

---

## 📝 Checklist

- [ ] PostgreSQL сервис создан на Railway
- [ ] Переменная `DATABASE_URL` присутствует в Variables
- [ ] `pg` добавлен в `package.json` (уже сделано)
- [ ] `DB_SYNC=true` установлен для первого запуска
- [ ] Redeploy выполнен
- [ ] В логах видно "Используется PostgreSQL"
- [ ] API работает
- [ ] Данные сохраняются после redeploy

---

**Готово! Теперь база данных не будет очищаться при деплое! ✅**

